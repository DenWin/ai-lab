# Hooks

Source-controlled hook definitions for AI harnesses.

Use `ai-artifacts/hooks/shared/`, `ai-artifacts/hooks/<vendor>/`, or `ai-artifacts/hooks/<vendor>/<harness>/` when a hook artifact itself
belongs in the repo. Hook-related skills remain under `ai-artifacts/skills/`.
