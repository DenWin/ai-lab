# 01 — Confirm grouping & introduce the `planning` group

Status: ready-for-human

## What to build

Confirm the group assignments in the PRD and the new top-level `planning` group for
to-issues / to-prd / triage. Once confirmed: add `/.claude/commands/planning/` to `.gitignore`
so the generated mirror for the new group is ignored.

## Acceptance criteria

- [ ] Group for each of the 9 imports confirmed (coding / planning / setup)
- [ ] Decision recorded: introduce `planning` group (yes/no)
- [ ] `.gitignore` updated with the new group's mirror path

## Blocked by

None — can start immediately. (Gates 03 and 04.)
