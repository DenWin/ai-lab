#!/bin/sh
# Thin shim: git hook filenames can't carry extensions, but PowerShell 7.6+
# refuses to run -File targets without a .ps1 extension. Delegate to the
# real logic in pre-commit.ps1 alongside this file.
exec pwsh -NoProfile -NonInteractive -File "$(dirname "$0")/pre-commit.ps1" "$@"
